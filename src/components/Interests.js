import React from "react"

function Brian(){
const name = 'Buttons allow users to take actions, and make choices, with a single tap. Buttons communicate actions that users can take. They are typically placed throughout'

  return(
  <div>
    <h3>Interests</h3>
    <p>{name}</p>
  </div>
  )
}

export default Brian